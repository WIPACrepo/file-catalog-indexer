version: 2.1

jobs:
  unit:
    docker:
      - image: icecube/icetray:combo-stable-devel
    steps:
      - checkout
      - run: pip3 install -r requirements.txt && pytest tests

workflows:
  build_and_test:
    jobs:
      - unit
