version: 2.1

jobs:
  unit:
    docker:
      - image: icecube/icetray:combo-stable-devel
    steps:
      - checkout
      - run: python3 -V && pip3 install -r requirements.txt && pytest tests

workflows:
  build_and_test:
    jobs:
      - unit
